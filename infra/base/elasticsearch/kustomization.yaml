apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - 01-namespace.yaml
  # Elasticsearch and Kibana CRs (managed by ECK operator)
  - 21-elasticsearch-cr.yaml
  - 31-kibana-cr.yaml

# labels:
# - includeSelectors: true
#   pairs:
#     app.kubernetes.io/managed-by: kustomize
#     app.kubernetes.io/part-of: elasticsearch-stack





