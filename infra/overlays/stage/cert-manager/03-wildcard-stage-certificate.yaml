apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-stage-example-com
  namespace: cert-manager
spec:
  secretName: wildcard-stage-example-com-tls
  issuerRef:
    name: letsencrypt-staging
    kind: ClusterIssuer
  dnsNames:
  - "*.stage.example.com"
  - "stage.example.com"